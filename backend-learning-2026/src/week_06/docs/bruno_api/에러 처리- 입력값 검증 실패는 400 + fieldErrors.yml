info:
  name: "에러 처리: 입력값 검증 실패는 400 + fieldErrors"
  type: http
  seq: 11

http:
  method: POST
  url: http://localhost:18080/api/boards
  body:
    type: json
    data: |-
      Content-Type: application/json

      {}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
