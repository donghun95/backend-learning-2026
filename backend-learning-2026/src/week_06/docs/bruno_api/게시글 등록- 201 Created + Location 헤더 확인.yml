info:
  name: "게시글 등록: 201 Created + Location 헤더 확인"
  type: http
  seq: 7

http:
  method: POST
  url: http://localhost:18080/api/boards
  body:
    type: json
    data: |
      {
        "title": "REST 리팩토링",
        "content": "6주차 시연-1",
        "writer": "donghun3"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
