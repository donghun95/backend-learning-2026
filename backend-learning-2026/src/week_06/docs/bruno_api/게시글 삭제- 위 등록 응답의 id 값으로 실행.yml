info:
  name: "게시글 삭제: 위 등록 응답의 id 값으로 실행"
  type: http
  seq: 8

http:
  method: DELETE
  url: http://localhost:18080/api/boards/111
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
