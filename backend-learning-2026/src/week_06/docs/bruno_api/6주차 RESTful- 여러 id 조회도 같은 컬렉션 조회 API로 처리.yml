info:
  name: "6주차 RESTful: 여러 id 조회도 같은 컬렉션 조회 API로 처리"
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:18080/api/boards?ids=1&ids=3&ids=5
  params:
    - name: ids
      value: "1"
      type: query
    - name: ids
      value: "3"
      type: query
    - name: ids
      value: "5"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
