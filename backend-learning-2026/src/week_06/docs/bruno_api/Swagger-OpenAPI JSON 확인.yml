info:
  name: Swagger/OpenAPI JSON 확인
  type: http
  seq: 13

http:
  method: GET
  url: http://localhost:18080/v3/api-docs
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
