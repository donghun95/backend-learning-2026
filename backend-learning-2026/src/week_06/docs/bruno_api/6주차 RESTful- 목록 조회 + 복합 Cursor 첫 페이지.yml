info:
  name: "6주차 RESTful: 목록 조회 + 복합 Cursor 첫 페이지"
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:18080/api/boards?size=2
  params:
    - name: size
      value: "2"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
