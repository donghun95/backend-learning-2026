info:
  name: "동적 검색: 제목 + 작성자"
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:18080/api/boards/search?title=Cursor&writer=donghun
  params:
    - name: title
      value: Cursor
      type: query
    - name: writer
      value: donghun
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
