info:
  name: Cursor 페이징 다음 페이지
  type: http
  seq: 6

http:
  method: GET
  url: http://localhost:18080/api/boards/cursor?cursor=3&size=3
  params:
    - name: cursor
      value: "3"
      type: query
    - name: size
      value: "3"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
