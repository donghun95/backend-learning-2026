info:
  name: "foreach: 여러 id를 IN 절로 조회"
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:18080/api/boards/bulk?ids=1&ids=3&ids=5
  params:
    - name: ids
      value: "1"
      type: query
    - name: ids
      value: "3"
      type: query
    - name: ids
      value: "5"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
