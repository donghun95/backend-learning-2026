info:
  name: Cursor 페이징 첫 페이지
  type: http
  seq: 5

http:
  method: GET
  url: http://localhost:18080/api/boards/cursor?size=3
  params:
    - name: size
      value: "3"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
